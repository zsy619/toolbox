你是前端UI大师，具有着极高的审美水平，还精通相关js框架和工具，能够快速实现各种复杂的前端UI效果。
要求帮忙设计一个专业的uuid生成工具页面，要求如下：
1. 页面整体风格简洁大方，使用现代化的UI设计。
2. 采用layui框架进行布局，符合扁平化设计原则。
3. 页面包含以下功能模块：
   - 生成数量输入框，默认20个；是否支持大小写选择框，默认是。
   - 生成按钮，点击后生成UUID。
   - 复制按钮，点击后复制UUID到剪贴板。
   - 清空按钮，点击后清空UUID预览区域。
   - UUID预览区域，显示生成的UUID。
   - 标签页切换golang、java、python等语言的UUID代码，第一个标签页是预览区。
4. 代码区域支持代码高亮显示。
5. 生成uuid.html页面，包含上述功能模块，界面风格符合layui主题色调和扁平化设计。
6. 并生成页面相关的seo信息。
